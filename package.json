{
  "name": "React-App",
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "start": "turbo run start:dev",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.13.0"
  }
}
